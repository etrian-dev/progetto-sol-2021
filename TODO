SERVER:

FIX read non consentite nonostante sia stato aperto con successo in mututa esclusione il file
(probabilmente per via di un test in readFile su condizione scritta prima di usare PID)
(o forse in find_file?)

Fix terminazione soft_term (SIGHUP)
Garantire atomicità della appendToFile (?)
Testare algoritmo rimpiazzamento cache (circa OK)

Testare/implementare LOCK/UNLOCK/REMOVE

insertFile:
memmove o memcpy? ragionevolmente non si sovrappongono, quindi direi memcpy,
        ma al fine di evitare bug difficili da rintracciare al momento ho preferito memmove
        NOTA: https://stackoverflow.com/questions/4480098/memmove-doesnt-move

cambiare PERMS_ALL_READ in PERMISSIONS in tutti i posti dove compare
ragione: più chiaro quali permessi setta

CLIENT:
        Se writeFile fallisce rimuovere il file creato dal fileserver (?)
        Altrimenti rimane un file presente nel server ma vuoto (o no, ma comunque senza i dati
        che si voleva scrivere). Ad es espulsione di file vuoti a seguito di una -w <dir>,... -D <savedir>

        Verificare memory leaks (?)

API:
        Mettere lock su struttura dati condivisa?
        (cioè client_info che memorizza l'associazione socket <=> path del socket)
